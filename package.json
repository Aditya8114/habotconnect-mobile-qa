{
  "name": "habotconnect-mobile-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Mock QA automation architecture for the HabotConnect Parent-LSA mobile application assessment.",
  "scripts": {
    "test": "jest --runInBand",
    "test:unit": "jest tests/unit --runInBand",
    "test:integration": "jest tests/integration --runInBand",
    "lint": "eslint src tests --ext .js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "performance": "node performance/performance-test.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "prettier": "^3.3.3"
  }
}
